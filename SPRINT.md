# Sprint Scope — "จบงานให้ได้ภายในวันนี้"

เอกสารนี้คือสัญญาว่า "เสร็จ" แปลว่าอะไรในสปรินต์นี้ ทุก subagent ต้องอ่านก่อนเริ่มงาน และห้ามประกาศว่างานเสร็จโดยไม่ตรงกับนิยามในนี้

## หลักการที่ทำให้จบได้จริง: แบ่งงานตาม "ต้องใช้ฮาร์ดแวร์หรือไม่" ไม่ใช่ตามขั้นตอน pipeline

v3 แบ่งงานเป็น architecture → develop → test → review แบบเรียงลำดับ ซึ่งดีสำหรับความถูกต้อง แต่ทำให้ทุกอย่างต้องรอกันเป็นทอด ๆ สำหรับเดดไลน์ 1 วัน ให้แบ่งเป็น 2 track ที่ **ทำขนานกันได้ ไม่บล็อกกัน**

---

## Track A — ไม่ต้องใช้ฮาร์ดแวร์ (ทำให้ "เสร็จจริง 100%" ได้ภายในวันนี้)

**นิยามคำว่าเสร็จ:** โค้ดเขียนครบ + unit test ผ่านจริง + `flutter analyze` ไม่มี warning + CI เขียว

| งาน | ทำไมไม่ต้องใช้ฮาร์ดแวร์ |
|---|---|
| `beacon_kit` Dart API + `platform_interface` | เป็นการประกาศสัญญา (interface) ล้วน |
| **ตัวถอดรหัส ADV packet** (iBeacon, Eddystone UID/URL/TLM, Ksensor) | เป็น pure function: byte array เข้า → object ออก ทดสอบด้วย fixture ได้ 100% |
| `domain/` ทั้งหมด (entities, repository interface, usecases) | pure Dart ตามกฎ Clean Architecture อยู่แล้ว |
| `presentation/` BLoC ทุกตัว (event/state/bloc) | ทดสอบด้วย `bloc_test` + mock usecase |
| Unit test + bloc_test ทั้งหมด | — |
| CI, lint, format | — |

**จุดสำคัญที่ทำให้ Track A มีค่ามาก:** Track A ครอบคลุม **ฟีเจอร์ broadcast (Path A) ทั้งฟีเจอร์** ซึ่งเป็นครึ่งหนึ่งของโปรดักต์จริง ๆ และเป็นครึ่งที่ใช้ได้กับ beacon ทุกยี่ห้อที่ broadcast ตามมาตรฐานเปิด — ไม่ใช่เศษงาน

---

## Track B — ต้องมีฮาร์ดแวร์จริงถึงจะ "ยืนยัน" ได้

**นิยามคำว่าเสร็จวันนี้:** โค้ดเขียนครบและคอมไพล์ผ่าน = **"code-complete (ยังไม่ verified)"** เท่านั้น — ห้ามเขียนว่า "เสร็จ" หรือ "ทำงานได้" เด็ดขาด

| งาน | ต้องใช้อะไรถึงจะยืนยันได้ |
|---|---|
| Android platform channel → `kbeaconlib2` (AAR) | เครื่อง Android จริง + beacon K9P จริง |
| iOS platform channel → `kbeaconlib2` (CocoaPods) | iPhone จริง + K9P จริง (**iOS Simulator ไม่มี Bluetooth เลย** — จำลองไม่ได้) |
| GATT connect / MD5 auth / อ่าน-เขียน config | K9P จริง + รู้ password ของเครื่องนั้น |
| อ่านประวัติ sensor / OTA | K9P จริง (OTA ต้องมีไฟล์เฟิร์มแวร์ที่ถูกต้องด้วย) |
| BLE runtime permission flow | เครื่องจริงทั้งสอง OS |

---

## ข้อห้ามเด็ดขาดของสปรินต์นี้

เมื่อเร่งงาน ความเสี่ยงที่ใหญ่ที่สุดไม่ใช่ "ทำไม่ทัน" แต่คือ **"รายงานว่าเสร็จทั้งที่ยังไม่ได้พิสูจน์"** ซึ่งอันตรายกว่ามาก เพราะทีมจะไปต่อบนสิ่งที่ไม่จริง

1. ห้าม subagent ตัวใดอ้างว่างาน Track B "ทำงานได้/ผ่านการทดสอบแล้ว" โดยไม่มีหลักฐานจากอุปกรณ์จริง (log จากเครื่องจริง หรือคนยืนยัน) — ถ้าไม่มี ให้เขียนว่า `code-complete, unverified — รอทดสอบกับ K9P จริง`
2. ห้ามนับ mock test ของ Track B เป็นการยืนยันว่าใช้งานได้ — mock พิสูจน์ได้แค่ว่าโค้ดเราเรียกถูกตามสัญญาที่เรา *คิดว่า* ถูก ไม่ได้พิสูจน์ว่าอุปกรณ์ตอบแบบนั้นจริง
3. ห้ามลดคุณภาพของ Track A เพื่อให้ทัน — Track A ไม่มีข้ออ้างเรื่องฮาร์ดแวร์ ถ้าเทสต์ไม่ผ่านคือยังไม่เสร็จ

## ลำดับความสำคัญเมื่อเวลาไม่พอ

ตัดจากล่างขึ้นบน:

1. ตัวถอดรหัส ADV + เทสต์ (หัวใจของ Path A — ห้ามตัด)
2. domain + BLoC ของฟีเจอร์ scan + เทสต์
3. CI เขียว
4. Android platform channel (code-complete)
5. iOS platform channel (code-complete)
6. OTA — ตัดได้เป็นอันดับแรก เพราะพึ่งฮาร์ดแวร์เต็มตัวและใช้ไม่บ่อย
