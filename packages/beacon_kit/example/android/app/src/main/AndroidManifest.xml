<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      android:name=".ExampleApplication" — **ไม่ใช้ ${applicationName} ที่ Flutter
      เติมให้** (ค่าปกติคือ android.app.Application)

      เหตุผล (ADR-14): เมื่อระบบส่งผลสแกนมาที่ BeaconScanReceiver ตอนแอปถูกปิดไปแล้ว
      ระบบจะสร้าง process ใหม่แล้วเรียก Application.onCreate() **ก่อน** onReceive()
      เสมอ — เป็นจุดเดียวที่รับประกันได้ว่าจะทำงานทุกครั้งที่ process เกิด ไม่ว่าจะ
      เกิดด้วยเหตุใด เครื่องมือเขียน log จึงต้องอยู่ที่นั่น ไม่ใช่ที่ Activity

      ระบุชื่อคลาสตรง ๆ แทนการตั้ง manifestPlaceholder เพื่อให้ตรวจได้จากไฟล์นี้ไฟล์
      เดียวว่าใครเป็น Application — placeholder ที่ปลั๊กอินอื่นเขียนทับได้จะกลายเป็น
      ความล้มเหลวเงียบ ๆ ที่ไม่มีใครเห็นจนกว่าจะไปทดสอบเบื้องหลังแล้วไม่มี log
    -->
    <application
        android:label="example"
        android:name=".ExampleApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
