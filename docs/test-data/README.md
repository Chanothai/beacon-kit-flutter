# ข้อมูลดิบจากการทดสอบบนอุปกรณ์จริง

เก็บไฟล์ log ดิบจากการทดสอบไว้ในรีโป เพื่อให้**การวิเคราะห์ทำซ้ำได้**

## ทำไมต้อง commit ไฟล์ดิบ ไม่ใช่เก็บแต่ข้อสรุป

ข้อสรุปที่ไม่มีข้อมูลดิบกำกับคือข้อสรุปที่ตรวจย้อนกลับไม่ได้ — ถ้าวันหนึ่งพบว่าวิธี
คำนวณของเราผิด (นับ artifact รวมไปด้วย ตัดช่วงผิด ใช้เขตเวลาผิด) จะต้องไปทดสอบใหม่
ทั้งรอบ ซึ่งการทดสอบหนึ่งรอบใช้เวลาข้ามคืนและควบคุมเงื่อนไขให้เหมือนเดิมไม่ได้

## วิเคราะห์ยังไง

```bash
dart run tool/analyze_region_log.dart docs/test-data/<ไฟล์>.log
```

สคริปต์เดียวกันนี้ใช้กับ log ทุกรอบ — ตัวเลขจากคนละรอบจะเทียบกันได้ก็ต่อเมื่อคำนวณ
ด้วยวิธีเดียวกัน

## ตรวจก่อน commit ไฟล์ log ใหม่ทุกครั้ง

รูปแบบ log ปัจจุบันมีแค่ timestamp / ตัวระบุ process / ชนิด event / region
identifier / ข้อสรุป / สัญญาณดิบ — **ไม่มี** proximity UUID, major/minor, พิกัด,
หรือข้อมูลระบุตัวบุคคล

คีย์ในคอลัมน์สัญญาณดิบที่ตรวจแล้วของสองไฟล์ปัจจุบัน:
- iOS (5 คอลัมน์): `launchKey` `everActive` `state` `uptime` `monitoredRegions`
- Android (6 คอลัมน์): `procUuid` `pid` `uptimeMs` `receiverEntry` `everForeground`
  `activities` `state` `importance` `doze` `battOpt` `restoredRegions`

`procUuid` เป็น UUID ที่ **แอปสุ่มเองตอน process เริ่ม** ไม่ผูกกับเครื่องหรือผู้ใช้
และไม่ใช่ proximity UUID ของ beacon

ถ้ารูปแบบ log เปลี่ยนในอนาคต ต้องตรวจใหม่ก่อน commit:

```bash
grep -ciE "[0-9a-f]{8}-[0-9a-f]{4}|@|\+66|[0-9]{2}\.[0-9]{4}" <ไฟล์>.log
```

⚠️ **สิ่งที่ log ยังบอกได้แม้ไม่มีข้อมูลส่วนบุคคลตรง ๆ:** เขตเวลาของผู้ทดสอบ และ
ช่วงเวลาที่เครื่องอยู่นิ่ง/ถูกใช้งานในคืนนั้น — ยอมรับได้สำหรับ repo ภายในบริษัท
แต่ถ้าจะเผยแพร่ออกนอกองค์กร ต้องพิจารณาใหม่

## ความจริงภาคสนาม = เกณฑ์รับ

**`GROUND_TRUTH.md` ในโฟลเดอร์นี้บันทึกว่า "คำตอบที่ถูกต้อง" ของแต่ละไฟล์คืออะไร**
ซึ่งเป็นสิ่งที่ log เองบอกไม่ได้ · ทั้งสองไฟล์เก็บภายใต้เงื่อนไขที่รู้คำตอบล่วงหน้า
แน่นอน (**enter 1 ครั้ง · exit 0 ครั้ง**) จึงเป็น **เกณฑ์รับของชั้นกรองกลาง**
ตัวกรองที่อ่านสองไฟล์นี้แล้วไม่ได้ผลตามตารางในไฟล์นั้น = ยังไม่ผ่าน

**ห้ามลบสองไฟล์นี้ และห้ามแก้เนื้อไฟล์** — เก็บซ้ำไม่ได้ (ใช้เวลาข้ามคืนต่อรอบ
และคุมเงื่อนไขให้เหมือนเดิมไม่ได้)

> 📒 **ก่อนตีความไฟล์ใดในตารางข้างล่าง อ่าน [`docs/beacon-inventory.md`](../beacon-inventory.md) ประกอบเสมอ**
> — ไฟล์หลักฐานบอกได้แค่ "อะไรถูกตรวจพบ" ไม่ได้บอกว่าบีคอนตัวไหนอยู่ที่ไหนหรือถูก
> ตั้งค่าใหม่เมื่อไร · การตีความผิดเพราะขาดข้อมูลนี้เกิดขึ้นจริงแล้วสองครั้งในวันเดียว

## รายการไฟล์

| ไฟล์ | วันที่ | แพลตฟอร์ม | สภาพการทดสอบ | ใช้ตอบเรื่องอะไร |
|---|---|---|---|---|
| `2026-08-30_overnight_region_flapping.log` | 30 ส.ค. – 31 ส.ค. 2026 (14 ชม.) | **iOS** (CoreLocation) | มือถือวางนิ่ง จอดับ ล็อกเครื่อง แอปอยู่เบื้องหลัง K9P วางนิ่งไม่ถอดแบต ไม่มีใครขยับอะไร | region flapping ระยะยาว (เช็คลิสต์ข้อ 16), ค่า debounce ใน ADR-11, หลักฐานสนับสนุนข้อ 13 และข้อ 14 |
| `2026-09-01_android_overnight_region_flapping.log` | 1 ก.ย. – 2 ก.ย. 2026 (18 ชม. 33 น.) | **Android** (`PendingIntent` + นาฬิกาปลุก) | เงื่อนไขเดียวกันเป๊ะกับไฟล์บน — มือถือวางนิ่ง จอดับ K9P วางนิ่งข้าง ๆ ไม่มีใครแตะทั้งคืน | ADR-15 (`exitTimeoutSeconds` เป็นขอบล่าง), การเกาะกลุ่มของเวลา `exit` กับ batch ของ `AlarmManager`, เกณฑ์รับของชั้นกรองกลางฝั่ง Android |
| `2026-09-03_android_overnight_stale_inside.log` | 2 ก.ย. – 4 ก.ย. 2026 (ช่วงหลักที่เกี่ยวข้อง: `enter` 3 ก.ย. 18:00:14 → เงียบสนิท 14 ชม. 14 น. → `exit`/`enter` เช้า 4 ก.ย. ~08:39-08:42) | **Android** (`PendingIntent` + นาฬิกาปลุก) — เครื่อง **Xiaomi/MIUI**, `battOpt=optimized` (ตรงตามที่ log บันทึกไว้ทุกบรรทัด) และผู้ทดสอบรายงาน **App Standby bucket = 40 (`rare`)** ณ ตอนทดสอบ (ไม่ได้อยู่ในคอลัมน์ log เพราะไฟล์นี้มาจากบิลด์ก่อนมีฟิลด์ `standbyBucket=`) | K9P ถูกนำออกนอกระยะสัญญาณ ~18:25-08:39 (มือถือพกออกจากโซน) ผู้ทดสอบยืนยันว่า **ไม่ได้แตะเครื่องเลยทั้งคืน** | หลักฐาน**บั๊กหลัก**ของ ADR-17 (สถานะ `inside` ค้างข้ามคืนเพราะนาฬิกาปลุก `exit` ไม่ถูกเรียกเลย — ไม่มี `exit` และไม่มี `enter` ตอนเช้าจริงตามที่ควรเกิด) ⚠️ **เป็นบิลด์ก่อนแก้** — `grep -c "exitReason=\|standbyBucket="` ได้ `0` ทั้งไฟล์ ยืนยันว่าไม่มีฟิลด์ใหม่ของ ADR-17 เลย จึง**ไม่ใช่**หลักฐานว่า `reconcile()`/K=10 ใช้ได้จริง เป็นแค่หลักฐานว่าบั๊กเกิดขึ้นจริงบนอุปกรณ์ |
| `2026-09-03_ios_overnight_exit_and_everactive.log` | 3 ก.ย. – 4 ก.ย. 2026 (91 บรรทัด · md5 `2906dc8bb271874be89116a1681718df`) | **iOS** (CoreLocation) | **iPhone 12 Pro Max (iPhone13,4) · iOS 26.3.1** — เวอร์ชัน ณ วันทดสอบยืนยันโดยผู้ทดสอบ (ไฟล์ log ฝั่ง iOS ยังไม่มีฟิลด์รุ่นเครื่องให้ตรวจย้อนกลับได้เอง ต่างจากฝั่ง Android) · บิลด์**หลัง PR #18** (มี ADR-16) · ติดตั้งใหม่ 3 ก.ย. 16:55 · บีคอน 2 ตัว (`bigc-test` + `k9p-default`) · พาเครื่องออกนอกระยะ ~18:25 กลับเข้าระยะ 08:38 · **ช่วงกลางวัน 4 ก.ย. ไม่ได้ตั้งใจทดสอบอะไร** (flapping ที่ขอบระยะ และพาเครื่องไปห้องน้ำ 09:54) — อย่านับช่วงนั้นเข้าสถิติ · ผู้ทดสอบดึงไฟล์เอง (Xcode → Devices and Simulators → Download Container หรือปุ่ม "ดู log" ในแอป — ไม่ได้บันทึกว่าใช้ทางไหน) | ผล runbook §11 ของ ADR-16 (เช็คลิสต์ข้อ 12: §11a/§11b/§11c) · **คู่เทียบคืนเดียวกันกับ `2026-09-03_android_overnight_stale_inside.log`** (อยู่ใน PR #19) ที่ใช้ยืนยันว่าเหตุการณ์จริงมีทั้ง `exit` และ `enter` ฝั่ง Android จึงพลาดของที่มีอยู่จริง |
| `2026-09-09_android_proximity_background.log` | 8 ก.ย. – 9 ก.ย. 2026 (422 บรรทัด · md5 `84a272e84afcbf9ee0749449e3e55670`) | **Android** (`PendingIntent` + `ProximityGate` ใน `BeaconScanReceiver`) | **Xiaomi 11T Pro (21081111RG) · Android 13 / API 33** · เดินเข้า-ออกระยะบีคอน K9P หลายรอบโดยแอปถูกฆ่า | หลักฐานตั้งต้นของ **ADR-20** — พิสูจน์ว่าชั้นที่ 2 ทำงานในโปรเซสที่ถูกปลุก (`uptimeMs=139`) และเป็นไฟล์ที่ใช้พิสูจน์ว่า `staleAfter = 10 วินาที` สั้นเกินไปสำหรับ Android (ช่วงห่างมัธยฐานของ batch 12.1–17.5 วินาที · 38 จาก 52 ช่วง > 10 วินาที) → ยกเป็น 60 วินาที |
| `2026-09-09_android_proximity_background_round3.log` | 9 ก.ย. 2026 (98 บรรทัด · md5 `4e5cc9ece81ccd4b3b730761929bfa51`) | **Android** เครื่องเดียวกัน (`fyrg9xf67h9ts84d`) | รอบเดินซ้ำหลังยก `staleAfter` เป็น 60 วินาที และหลังแก้ notification บน MIUI | ผลของการยก `staleAfter` (`reason=stale` 50% → 25% · transition ที่มี `from` จริง 1 → 18) และหลักฐานเรื่อง `event=notification` `posted=`/`reason=` |
| `2026-09-10_ios_proximity_walk.log` | 10 ก.ย. 2026 (54 บรรทัด · md5 `02cb326397ef28d8eb8847b16627f910` · บิลด์ `build=a8f7fa1`) | **iOS** (CoreLocation ranging + `ProximityGate.swift`) | **iPhone 12 Pro Max (`iPhone13,4`) · iOS 26.6.1 (23G83)** · สิทธิ์ `Always` · บีคอน K9P 2 ตัว (`9902/2`, `9903/3`) · เดินเข้า-ออก แล้วปิดแอปจนถูกฆ่า | **รอบที่ 1 ของ ADR-21** — พิสูจน์ว่าชั้นที่ 2 ทำงานในโปรเซส `relaunchedFromTerminated` และ state รอดข้ามโปรเซส · **และหักล้างสมมติฐานเรื่อง `didFailRangingFor`** (`event=rangefail` = 0 บรรทัดทั้งไฟล์) |
| `2026-09-10_ios_proximity_counters.log` | 10 ก.ย. 2026 (62 บรรทัด · md5 `30620e50ca8d0d5fa801c05c3cf23a6b` · บิลด์ `build=4c6f648`) | **iOS** เครื่องเดียวกัน | **iPhone 12 Pro Max · iOS 26.6.1** · เดินสองช่วงตามบรีฟ: foreground 15:15–15:22 (โปรเซส `735217be`) แล้วปิดแอปจนถูกฆ่า → ระบบปลุกเอง 15:26–15:35 (โปรเซส `cbf514a3`) | **รอบที่ 2 ของ ADR-21 และเป็นแหล่งความจริงทั้งหมดของ ADR-22** — ตัวนับ `rangeCb=`/`inArray=`/`unknown=` บนบรรทัด `rangetick` ตอบว่าโหมด background ให้ callback ทุก **16.2 วินาที** · `unknown` **65%** · sample ที่ใช้ได้ **ต่อ key ทุก ~77 วินาที** · หน้าต่างหลังถูกปลุกยาว **8 นาที 23 วินาที** ⚠️ **เดินก่อนที่ ADR-21 จะบังคับ foreground-only** — บรรทัด `proximity` ที่ `state=background` คือหลักฐานว่ากลไก*เคย*ทำงาน ไม่ใช่พฤติกรรมของบิลด์ปัจจุบัน |
| `2026-09-10_ios_proximity_background_poc.log` | 10 ก.ย. 2026 (51 บรรทัด · md5 `513047fda38be6edb4994aaec9239a75` · บิลด์ `build=ead3b02`) | **iOS** เครื่องเดียวกัน — **บิลด์แรกที่มี ADR-22** (gate ตัวที่สองแบบ passthrough) | **iPhone 12 Pro Max · iOS 26.6.1** · สิทธิ์ `Always` + notification granted · **บีคอน K9P ตั้ง advertising interval 300 ms** (เงื่อนไขของผล) · เดิน 2 รอบตามบรีฟ: หยุดที่ **1.5 ม.** (17:24) และ **8 ม.** (17:27) โดยแอปถูกปัดทิ้งจนระบบฆ่าแล้วปลุกเอง | **POC ของ ADR-22** — latency `enter` → `proximity` แรก = **1.01 วินาที ทั้งสองรอบ** · รอบ 1.5 ม. ได้ `bucket=near` + `event=notification` ในโปรเซส `relaunchedFromTerminated` (`everActive=false`) · รอบ 8 ม. ได้ `far` และ**ไม่ยิง ตามที่คาด** · ยืนยัน `regionExit` ล้างทั้งสอง gate · **และเปิดข้อจำกัดของ `windowSize = 1`**: `near` พลิกเป็น `far` ใน 2 วินาทีขณะยืนนิ่ง (ข้อ 19.11) ⚠️ เป็น **slice** ของไฟล์เดียวกันบนเครื่อง (ทั้งไฟล์ 113 บรรทัดตอนดึง) เริ่มที่บรรทัด `launch` แรกของบิลด์ `ead3b02` |
| `2026-09-11_android_overnight_reconcile_redmi.log` | 10 ก.ย. 08:30 – 11 ก.ย. 09:34 2026 (971 บรรทัด · md5 `75e35bc2ecc7a1c30c79160122922688`) | **Android** (`PendingIntent` + `ProximityGate` ใน `BeaconScanReceiver`) | **Redmi Note 9 (`M2003J15SC`) · `0703455c0406`** — ⚠️ **คนละเครื่องกับ Xiaomi 11T Pro ที่ใช้ทำงาน notification เมื่อ 9 ก.ย. ห้ามปนผล** · `battOpt=optimized` · `standbyBucket` เปลี่ยนจาก `workingSet` (กลางวัน) เป็น `rare` (ข้ามคืน) · **advertising interval: ดู `ios_broadcast_scanning.md` หัวข้อ "interval ของบีคอน" — บีคอนตัวเดียวกันถูกปรับ 1000→300 ms เมื่อ 10 ก.ย. 16:35 ระหว่างช่วงที่ไฟล์นี้ครอบคลุมพอดี** | **หลักฐานรอบแรกของ `reconcile()` เคสข้ามคืน (ADR-17 · เช็คลิสต์ข้อ 10)** — `exitReason=staleReconcile` 11 ก.ย. 08:39:19 ที่ `sinceLastSeenMs=52172804` (**14 ชม. 29 นาที**) · และเป็นหลักฐานว่า **นาฬิกาปลุก `exit` ไม่ทำงานข้ามคืนจริง** (`exitReason=alarm` 62 ครั้งตอนกลางวัน แต่ **0 ครั้ง**ในหน้าต่าง 18:10→08:39) ⚠️ **บิลด์นี้ไม่มีฟิลด์ `build=`** จึงแยกบิลด์จากไฟล์เองไม่ได้ — **318 บรรทัดแรกมาจากบิลด์ก่อน 10 ก.ย. 13:37** (สังเกตจากการที่ยังไม่มีคอลัมน์ `store=`) |
| `2026-09-11_ios_overnight_background_proximity.log` | 10 ก.ย. 18:10 – 11 ก.ย. 09:07 2026 (33 บรรทัด · md5 `8d50dabff5f0815fcdec1e7579e962bc` · บิลด์ `build=ead3b02`) | **iOS** (CoreLocation + ADR-22 gate ตัวที่สอง) | **iPhone 12 Pro Max · iOS 26.6.1** · สิทธิ์ `Always` · **advertising interval 300 ms** · วางเครื่องไว้ข้ามคืนแล้วพกกลับเข้าระยะเช้า 11 ก.ย. | **ทำซ้ำ 19.7ก เป็นรอบที่ 2** — `notification mode=bg` 08:41:52 ในโปรเซส `relaunchedFromTerminated` + `everActive=false` · **และหักล้างเพดาน "8 นาที 23 วินาที" ของ ADR-21 หัวข้อ 1**: โปรเซส `46b89549` มีชีวิตและ ranging เดินต่อเนื่อง **27 นาที 57 วินาที** (08:39:13→09:07:10 · `uptimeMs` 11→1,676,879) · ⚠️ **ตัวเลข latency/`unknown` ต่างจากรอบ POC มากทั้งที่บิลด์และ interval เดียวกัน** (ดู `ios_broadcast_scanning.md` หัวข้อ "interval ของบีคอน" ตาราง 4 แถว) |

⚠️ **ชื่อไฟล์ไม่สมมาตร** — ไฟล์ของ iOS ตั้งชื่อไว้ก่อนที่จะมีสองแพลตฟอร์ม จึงไม่มี
คำว่า `ios` อยู่ในชื่อ **จงใจไม่เปลี่ยนชื่อ** เพราะมีเอกสารหลายฉบับอ้างถึงด้วยชื่อนี้
(`ARCHITECTURE.md`, runbook, `tool/analyze_region_log.dart`) ไฟล์ใหม่ทุกไฟล์ต้องมี
แพลตฟอร์มอยู่ในชื่อ
