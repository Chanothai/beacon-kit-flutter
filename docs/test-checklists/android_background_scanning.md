# สถานะการทดสอบ — การทำงานเบื้องหลังบน Android

**ไฟล์นี้เป็นเจ้าของสถานะแต่เพียงผู้เดียว** ขั้นตอนการทดสอบอยู่ที่
[`android_background_runbook.md`](./android_background_runbook.md)
(กติกาเดียวกับคู่ `ios_broadcast_scanning.md` / `ios_device_test_runbook.md`)

---

## สภาพแวดล้อมที่ต้องระบุทุกครั้งที่กรอกผล

ผลที่ไม่ระบุหกตัวนี้ **ตรวจสอบย้อนกลับไม่ได้ และห้ามนับเป็นหลักฐาน**

| รายการ | ค่า |
|---|---|
| รุ่นเครื่อง | Redmi Note 9 (`M2003J15SC`, Xiaomi) |
| เวอร์ชัน Android | 12 (SDK 31) |
| เวอร์ชัน MIUI | `V13.0.2.0.SJOMIXM` |
| MIUI Autostart | _(ผู้ทดสอบกรอกทุกรอบ)_ |
| Battery optimization | _(ผู้ทดสอบกรอกทุกรอบ — ดูฟิลด์ `battOpt=` ใน log)_ |
| **advertising interval ของบีคอนที่ใช้ทดสอบ** | _(ผู้ทดสอบกรอกทุกรอบ — **ห้ามเว้น** เพิ่ม 10 ก.ย. 2026)_ |

📒 **ค่าปัจจุบันของบีคอนทุกตัว + ประวัติการเปลี่ยนแปลง อยู่ที่
[`docs/beacon-inventory.md`](../beacon-inventory.md)** — ต้องอ่านประกอบทุกครั้งที่
ตีความผลของช่วงเวลาใดช่วงเวลาหนึ่ง

> 🔴 **`advertising interval` เป็นช่องบังคับด้วยเหตุผลเดียวกับ MIUI Autostart เป๊ะ**:
> เป็นสวิตช์**นอกโค้ด**ที่ไม่ปรากฏในไฟล์ log เลย และ **10 ก.ย. 2026 พิสูจน์แล้วว่ามัน
> เปลี่ยนผลได้** — บีคอน K9P ถูกปรับจาก **1000 ms → 300 ms เมื่อ 16:35** ซึ่งอยู่คั่นกลาง
> ระหว่างสองรอบทดสอบฝั่ง iOS ที่เอาตัวเลขมาเทียบกัน (ดู `ios_broadcast_scanning.md`
> หัวข้อ "interval ของบีคอน") · **ทุกรอบก่อนหน้านั้นทั้ง iOS และ Android — รวมรอบข้ามคืน
> 3 ก.ย. และรอบ proximity 8-9 ก.ย. — ใช้ 1000 ms** ตัวเลขในไฟล์นี้จึงเป็นของ 1000 ms ทั้งหมด

(รุ่น/เวอร์ชันสามข้อบนยืนยันจาก `adb shell getprop` เมื่อ 31 ส.ค. 2026)

---

## สถานะรวม — แยกสองระดับตาม ADR เพราะยืนยันบนอุปกรณ์จริงไปคนละระดับ

### ADR-14 (background monitoring พื้นฐาน) — `observed`

มีหลักฐานจากอุปกรณ์จริงแล้ว 2 ไฟล์เท่านั้น (ห้ามอ้างไฟล์อื่นในโฟลเดอร์เดียวกันเป็น
หลักฐานฝั่ง Android — อีก 2 ไฟล์ในโฟลเดอร์เป็นของ iPhone และ `GROUND_TRUTH.md` เป็น
เอกสารกำกับ ไม่ใช่ log):

| ไฟล์ | บรรทัด | ช่วงเวลา | event | md5 |
|---|---|---|---|---|
| [`docs/test-data/2026-09-01_android_overnight_region_flapping.log`](../test-data/2026-09-01_android_overnight_region_flapping.log) | 219 | 09-01T14:14:48 → 09-02T08:48:34 | enter 63 · exit 62 · launch 94 | `f00be40f70c14e3058021ed6c484b471` |
| [`docs/test-data/2026-09-03_android_overnight_stale_inside.log`](../test-data/2026-09-03_android_overnight_stale_inside.log) | 279 | 09-02T09:34:11 → 09-04T10:46:21 | enter 53 · exit 48 · launch 178 | `2e5876af10de791bff0cc80d49bf24d6` |

⚠️ **ใช้คำว่า `observed` ไม่ใช่ `verified` โดยตั้งใจ** — ทั้งสองไฟล์เก็บภายใต้เงื่อนไข
"วางเครื่องนิ่งข้ามคืน" **ไม่ได้เดินตาม protocol เฉพาะเคสของแต่ละข้อใน
[ตารางผล](#ตารางผล) ด้านล่าง** จึงตอบได้แค่บางแถวเท่านั้น (ดูตารางผล — ส่วนใหญ่ยังว่าง
เพราะต้องอาศัยการกระทำของผู้ทดสอบ เช่น ปัดแอปทิ้ง/force-stop/รีบูต/สลับสวิตช์ ที่ log
สองไฟล์นี้ไม่มี) และยังไม่ครบเกณฑ์ทำซ้ำ 2-3 รอบต่อเคสตามกติกาของ
`android_background_runbook.md`

⚠️ **หมายเหตุความต่างของสองไฟล์ที่พบระหว่างตรวจ:** ไฟล์ 09-01 (`_region_flapping`)
เป็นบิลด์ที่**เก่ากว่า** ไฟล์ 09-03 อีกชั้น — บรรทัด `exit` ในไฟล์ 09-01 **ไม่มีคีย์
`sinceLastSeenMs`/`scheduledAtElapsed`/`firedAtElapsed` เลยสักบรรทัด** (62/62) ในขณะที่
ไฟล์ 09-03 มีครบทุกบรรทัด (48/48) — ยืนยันด้วย:
```bash
grep -c $'\texit\t' docs/test-data/2026-09-01_android_overnight_region_flapping.log   # 62
grep $'\texit\t' docs/test-data/2026-09-01_android_overnight_region_flapping.log | grep -c "sinceLastSeenMs="   # 0
grep -c $'\texit\t' docs/test-data/2026-09-03_android_overnight_stale_inside.log       # 48
grep $'\texit\t' docs/test-data/2026-09-03_android_overnight_stale_inside.log | grep -c "sinceLastSeenMs="   # 48
```
แปลว่าไฟล์ 09-01 มาจาก log schema คนละรุ่นกับ 09-03 — ห้ามคำนวณ delay ของ `exit`
(`sinceLastSeenMs` เป็นต้น) จากไฟล์ 09-01 เด็ดขาด มีแค่ไฟล์ 09-03 เท่านั้นที่ใช้คำนวณ
ตัวเลขนี้ได้

### ADR-17 (`reconcile()`) — `code-complete, unverified`

**ยังไม่เคยรันบนเครื่องจริงเลยแม้แต่ครั้งเดียว** — log ทั้งสองไฟล์ข้างบนมาจากบิลด์
**ก่อน** ADR-17 ยืนยันด้วยการรันจริง (4 ก.ย. 2026):

```bash
grep -c "exitReason=" docs/test-data/2026-09-01_android_overnight_region_flapping.log
# → 0
grep -c "exitReason=" docs/test-data/2026-09-03_android_overnight_stale_inside.log
# → 0
```

ทั้งสองไฟล์ไม่มีคีย์ `exitReason=` แม้แต่บรรทัดเดียว — ยืนยันว่าเป็นบิลด์ก่อน ADR-17 จริง
ตามที่คาดไว้ ดูแถว **ข้อ 10 (§11)** ในตารางผลด้านล่าง — สถานะยังคง **ยังไม่ทดสอบ**

### ADR-20 (proximity gate เบื้องหลัง) — `observed` เฉพาะ 4 ข้อ · ค่า `staleAfter` ใหม่ยัง `code-complete, unverified`

**บิลด์ที่ใช้ทดสอบ 9 ก.ย. 2026 (รอบเดิน 12:54-13:08):**
`sha256 099223dda38c7e63f714144a5a2ce6d1272000e18445ed4f7b072164e08302be` — บิลด์นี้ใช้
`staleAfterMillis = 10_000L`

**บิลด์รอบเดินที่ 2 (13:30-13:50) — `staleAfterMillis = 60_000L`:**
`sha256 ea7374ed118359dbcdf02cb29bac3026455bf4fdf0fd0552c245ac49b3f7677a` (build 13:17)

**บิลด์ที่ติดตั้งอยู่บนเครื่องตอนนี้ และที่รอบข้ามคืนคืน 9 ก.ย. จะรันบนมัน:**
`sha256 222566b794ce869704d18b0ac53087736e254460ed6e82ebb26a50162a0bcad5`
(`flutter build apk --debug` 9 ก.ย. 2026 13:55) — ต่างจากบิลด์ 13:17 ที่ **ตัวกรอง
notification ของ `ExampleProximityWatcher` เท่านั้น** (ยิงเฉพาะตอนเข้าสู่ความใกล้จาก
`far`/ไม่เคยมี bucket ไม่ยิงตอน `near`↔`immediate` ขยับกันเอง) **ไม่แตะตรรกะ SDK เลย**
· ยังไม่มีรอบเดินซ้ำหลังเปลี่ยน จึงเป็น `code-complete, unverified`
· ADR-17 ไม่ถูกแตะในบิลด์นี้ (ยืนยันจาก `git diff` — `BackgroundRegionMonitor.kt` 0 บรรทัด)
รอบข้ามคืนจึงยังนับเป็น**รอบที่ 2 ของ ADR-17**ได้

**ไฟล์หลักฐาน:** [`docs/test-data/2026-09-09_android_proximity_background.log`](../test-data/2026-09-09_android_proximity_background.log)
— 422 บรรทัด · md5 `84a272e84afcbf9ee0749449e3e55670` · 195 บรรทัด `event=proximity`
(ครอบทั้งรอบที่ 1 และรอบที่ 2)

| ข้อ | ผล | หลักฐาน |
|---|---|---|
| 1. เห็น `enter` ทั้งสอง region | **ผ่าน** | 12:44:21 `bigc-test` · 12:44:22 `k9p-default` |
| 3. เดินเข้า 2 m → **บรรทัดหลักฐาน**โดยไม่เปิดแอป | **ผ่าน** | 13:05:03 `bigc-test bucket=near from=none reason=closer medianM=1.2 rssi=-53 txPower=-51` ใน process `3ab5154f` ที่ `conclusion=relaunchedFromTerminated` |
| 3ก. เดินเข้า 2 m → **notification** โดยไม่เปิดแอป | ❌ **ยังพิสูจน์ไม่ได้** | ดูหัวข้อ "notification ถูกระบบบล็อก" ด้านล่าง — `dumpsys notification` รายงาน `numEnqueuedByApp=10 numPostedByApp=0 numBlocked=10` สำหรับแพ็กเกจนี้ |
| 4. เดินออก → ไม่มี notification ซ้ำ | **ผ่านบางส่วน** | 13:07:52 `bucket=far reason=farther medianM=5.2` ถูกกรองไม่ยิง notification ถูกต้อง **แต่**ตอนยืนนิ่งกลับได้ notification ซ้ำ (ดูข้อ 7 ด้านล่าง) |
| 5. `event=proximity` + `conclusion=relaunchedFromTerminated` | **ผ่าน** | 12:58:55.830 `uptimeMs=139` · 13:04:01.363 `uptimeMs=192` และอีกหลายบรรทัด |
| 6. `enter`/`exit` ไม่ regression | **ผ่าน** | 13:00:57 `exit` ทั้งสอง region (`exitReason=alarm` `sinceLastSeenMs=109713`) → 13:01:25 `enter bigc-test` → 13:01:32 `enter k9p-default` ครบวงจรตามปกติ |
| 2. ปัดแอปทิ้งจาก recent | **ผ่าน** | ทุกบรรทัดตั้งแต่ 12:58:22 เป็นต้นไปมี `everForeground=false activities=0 state=noActivityEver` และ `procUuid` ใหม่ทุกครั้ง |

**รอบเดินที่ 2 (13:30-13:50 · `staleAfter=60s`) — เทียบกับรอบที่ 1 โดยตรง:**

| ตัวชี้วัด | รอบ 1 `staleAfter=10s` (14 นาที) | รอบ 2 `staleAfter=60s` (20 นาที) |
|---|---|---|
| `proximity` ต่อนาที | 4.4 | 1.8 |
| `reason=stale` | 31/62 = 50% | 9/36 = 25% |
| `from=none` (state ถูกล้าง) | 30/62 = 48% | 9/36 = 25% |
| transition ที่มี `from` จริง (hysteresis ได้ทำงาน) | **1** | **18** |
| flap `near`↔`immediate` | 1 | **16** |
| notification ที่ควรเด้ง | 0.8 ใบ/นาที | 0.8 ใบ/นาที |

การขยับ `staleAfter` **ได้ผลตามที่ตั้งใจ** (สามแถวแรก) และ `reason=stale` ที่เหลือ 9 ครั้ง
ส่วนใหญ่ถูกต้อง (เกิดหลัง `exit` จริงช่วง 13:34-13:39) · `enter`/`exit` จับคู่ครบ 6/6 คู่
ไม่มี regression

⚠️ **แต่มันเปิดโปงปัญหาคนละตัวที่ถูกกลบอยู่:** ขอบ `immediate`/`near` **ไม่มี hysteresis**
(`classify()` ใส่ dead zone เฉพาะขอบ far/close ตาม ADR-19 หัวข้อ 6(ข)) ผู้ทดสอบที่ยืนนิ่ง
ราว 1 เมตรทำให้ median ข้าม `immediateMeters = 1.0` ไป-กลับทุก batch และทั้งสองทิศยิง
notification ได้เหมือนกัน → **16 จาก 36 event ของรอบที่ 2** เช่น 13:46:22 `immediate←near
medianM=0.8` → 13:49:25 `near←immediate medianM=1.7` → 13:49:53 `immediate←near medianM=0.8`

**`proximity_gate.dart` มีพฤติกรรมเดียวกันเป๊ะ** — เป็นช่องว่างของการออกแบบ ไม่ใช่บั๊กของ
การ port การเติม hysteresis ที่ขอบนี้ต้องเป็น ADR รอบใหม่ที่แก้ทั้ง Dart และ Kotlin พร้อมกัน
รอบนี้จึงกันที่ชั้นนโยบายของ example app แทน (ยิงเฉพาะตอนเข้าสู่ความใกล้) **ยังไม่มีอะไร
กันในตัว SDK** และ **ยังไม่ได้ทดสอบซ้ำหลังแก้**

### 🔎 สอบสวนก่อนบันทึก ADR-20 เป็น `observed` — ผลคือ **ยังบันทึกไม่ได้**

ไฟล์ที่สอบสวน: [`docs/test-data/2026-09-09_android_proximity_background_round3.log`](../test-data/2026-09-09_android_proximity_background_round3.log)
(98 บรรทัด · md5 `4e5cc9ece81ccd4b3b730761929bfa51` · ช่วง 16:41-16:51 ของเครื่องที่ 1)

#### A. `reason=stale` 3 บรรทัดใน 34 ms ที่ `k9p-default` (16:45:20.702/.720/.736)

**ไม่ใช่บั๊ก entry ซ้ำ** — สามบรรทัดมี `from=near`, `from=immediate`, `from=near`
ซึ่งเป็น `confirmedBucket` ของ **สาม gate key ที่ต่างกัน** (คนละ MAC ใน region
เดียวกัน ตาม ADR-20 หัวข้อ 3) ไม่ใช่ key เดียวยิงสามครั้ง ยืนยันโครงสร้างด้วยเทสต์
ใหม่ 2 ตัว: store เก็บเป็น `JSONObject` ที่ key ซ้ำไม่ได้โดยนิยาม (`save()` ซ้ำ 3 รอบ
→ `load()` ได้ 3 entry ไม่ใช่ 9) และ `sweepStale()` ยิงได้ **key ละครั้งเดียว**

**แต่เจอของจริง 2 อย่างระหว่างสอบสวน:**

1. **บรรทัดหลักฐานแยกบีคอนไม่ออก** — `ProximityChangedEvent` ไม่มีตัวระบุบีคอน
   (ADR-20 หัวข้อ 5 ไม่ได้นิยามไว้) บรรทัดของคนละบีคอนจึงพิมพ์ออกมาเหมือนกันหมด
   ทำให้อ่านเหมือนบั๊ก **แก้แล้ว: เพิ่ม `beacon=` (สองไบต์ท้ายของ MAC เท่านั้น
   ไม่ใช่ที่อยู่เต็ม เพราะไฟล์นี้ถูก commit เข้า repo)**
2. **`ProximityGateStore.clear()` ไม่มีผู้เรียกแม้แต่รายเดียว** — `stop()` ของ
   `BackgroundRegionMonitor` เรียก `BackgroundRegionStore.clearAll()` ซึ่งเป็น
   **store คนละตัว** สถานะชั้น 2 จึงค้างบนดิสก์ข้ามรอบทดสอบตลอดไป แล้วโผล่เป็น
   `stale` รัว ๆ ตอน sighting แรกของรอบถัดไป — ตรงกับรูปแบบที่เห็นพอดี (process
   เกิด 16:41:04 · sighting แรก 16:45:20) **แก้แล้ว: `monitorStop` ล้าง store ให้**
   ⚠️ แก้ที่ example app เพราะรอบนี้ห้ามแตะเส้นทาง region monitoring —
   **เป็นหนี้ที่ต้องย้ายเข้า `stop()` ของ SDK ในรอบถัดไป**

#### B. `from=none` ที่ 16:47:58 หลัง `bucket=far` ที่ 16:47:14 (process `e61faf74` เดียวกัน)

**บิลด์บนเครื่อง:** `sha256 5cb9d8b9a275e0dde33cf9a38fea6007003f4251710e3899588164700ba52a30`
· `lastUpdateTime=2026-09-09 15:53:38` — **ไม่ตรงกับบิลด์ใดที่สร้างในเซสชันนี้**
(สร้างไว้ 12:37 / 13:17 / 13:55 / 14:46) แปลว่ามีการ build+install จากที่อื่นเวลา 15:53
**แต่ระบุรุ่นได้จากเนื้อ log**: ไฟล์มีบรรทัด `event=notification` ซึ่งมีเฉพาะตั้งแต่
commit `d217f21` เป็นต้นไป → บิลด์นี้รวม `c66f33b` แล้ว → **`staleAfterMillis = 60_000L`**

ช่องว่าง 16:47:14 → 16:47:58 = **44.5 วินาที < 60 วินาที** จึงไม่ควร stale และไม่ควร
ได้ `from=none` — เว้นแต่เป็น **คนละ key** ซึ่งเป็นคำอธิบายที่น่าจะใช่ที่สุดเพราะข้อ A
พิสูจน์แล้วว่า `k9p-default` มีบีคอนอย่างน้อย 3 ตัว

**code path ที่ทำให้ state หายโดยไม่ emit `stale` — มีจริง 3 ทาง (พบจากการอ่านโค้ด):**

| ทาง | ผลลัพธ์ | ร่องรอยเดิม |
|---|---|---|
| `load()` โยน exception | `lastError` ถูกตั้ง แล้วคืน map ว่าง → push ถัดไปได้ `from=none` | logcat เท่านั้น |
| `statesFromJson()` ข้าม key ที่ถอดไม่ออก (`continue`) | key นั้นหายเงียบ ๆ key เดียว | **ไม่มีเลย** |
| `save()` คืน false / โยน exception | สถานะไม่ลงดิสก์ → process ถัดไปเริ่มจากศูนย์ | logcat เท่านั้น |

ทั้งสามทาง**ไม่เคยปรากฏในไฟล์หลักฐาน** ตอนอ่านผลย้อนหลังจึงแยกจาก "คนละบีคอน" ไม่ได้
**แก้แล้ว: เพิ่ม `store=ok|<error>` ในบรรทัด `proximity`**

**สรุปข้อ B: อธิบายได้ แต่ยังพิสูจน์ไม่ได้จากไฟล์ที่มี** — ต้องเดินอีกหนึ่งรอบบนบิลด์
ที่มี `beacon=`/`store=` ถึงจะชี้ขาดว่าเป็นคนละบีคอนหรือเป็น store ล้มเหลว

#### C. มีการรัน `set-standby-bucket` ซ้ำระหว่าง 16:47-16:51 หรือไม่

**ไม่มี** — คำสั่ง `adb` ครั้งสุดท้ายที่ยิงไปยังเครื่องที่ 1 ในเซสชันนี้คือเวลาประมาณ
14:25 (ก่อนสลับสาย) และ `am set-standby-bucket` ครั้งสุดท้ายบนเครื่องนี้คือหลังการ
ติดตั้งซ้ำเวลาประมาณ 14:21 · ระหว่าง 16:47-16:51 **ไม่มีคำสั่งใดถูกยิงไปที่เครื่องนี้เลย**
ค่าที่เห็นในบรรทัด log ช่วงนั้นจึงเป็นค่าที่ระบบกำหนดเอง ไม่ใช่ผลของคำสั่งจากภายนอก

#### D. `event=monitorStart` / `event=monitorStop`

เพิ่มแล้วในไฟล์หลักฐาน (`detail=registered=N failed=M` และ `detail=byUser`) ปิดช่องที่
ทำให้ "ความเงียบ" แปลได้สองอย่าง: ระบบไม่ปลุกแอป หรือไม่มีใครสั่งเฝ้าตั้งแต่แรก ·
`monitorStop` ล้าง `ProximityGateStore` ให้ด้วย (`proximityStoreCleared=true`)

#### E. สถานะของ ADR-20 หลังสอบสวน

**ยังเป็น `code-complete, unverified` ห้ามเลื่อนเป็น `observed`** — ข้อ A มีคำอธิบาย
ครบและมีเทสต์ล็อกแล้ว แต่**ข้อ B ยังพิสูจน์ไม่ได้** ด้วยไฟล์ที่มีอยู่ และการเลื่อน
สถานะโดยที่ยังมีบรรทัดที่อธิบายไม่ได้ค้างอยู่ ขัด `CONTRIBUTING.md` ข้อ 4 โดยตรง
· เงื่อนไขที่จะเลื่อนได้: เดินหนึ่งรอบบนบิลด์ที่มี `beacon=`/`store=` แล้วบรรทัดทุก
บรรทัดในรอบนั้นอธิบายได้

---

### เครื่องที่ 2 — Xiaomi 11T Pro `21081111RG` (แยกจาก Redmi Note 9 ห้ามปนผล)

| | เครื่องที่ 1 | เครื่องที่ 2 |
|---|---|---|
| serial | `0703455c0406` | `fyrg9xf67h9ts84d` |
| รุ่น | M2003J15SC (Redmi Note 9 · `merlin`) | **21081111RG (Xiaomi 11T Pro · `amber`)** |
| Android / API | 12 / **31** | 13 / **33** |
| MIUI | — | **V140 (`V14.0.6.0.TKWMIXM`)** |
| ใช้ทำอะไร | รอบข้ามคืน ADR-14/ADR-17 | **พิสูจน์ notification บน API 33** |
| `lightIdle` ในไฟล์ log | `unsupported-api31` เสมอ | **`false` (ค่าจริงครั้งแรก)** |

**⚠️ ผลของสองเครื่องห้ามนำมารวมกันเป็นรอบเดียว** — คนละ API level คนละกลไก
notification และคนละไฟล์หลักฐาน

#### notification บน API 33 — `observed` (9 ก.ย. 2026 14:47)

สาเหตุที่ notification ไม่ขึ้นบนเครื่องนี้ **คนละเรื่องกับ Redmi**: Android 13 เปลี่ยน
`POST_NOTIFICATIONS` เป็น runtime permission และ `targetSdk` ของ example (จาก
`flutter.targetSdkVersion`) ≥ 33 → **ระบบไม่ถามให้เอง** แอปต้องขอเอง แต่โค้ดเดิม
`requestNotificationAuthorization` คืนแค่ `SDK_INT < 33` = ไม่เคยขอเลยสักครั้ง
ผลคือ `notify()` เงียบสนิท ไม่ throw ไม่มี error (แก้ใน commit `d217f21`)

**ยืนยันครบ 3 ชั้นตามที่กำหนด:**

| ชั้น | ผล |
|---|---|
| เห็นด้วยตา | heads-up ขึ้นบนจอจริง "ทดสอบแจ้งเตือน · ถ้าเห็นใบนี้ = ระบบยอมให้แอปแจ้งเตือนแล้ว" |
| `dumpsys notification` | `numEnqueuedByApp=18 numPostedByApp=6` (เดิม posted=0) · `NotificationRecord … importance=4` · `effectiveNotificationChannel{mId='beacon_kit_example.region_events_v2', mImportance=4}` · ไม่มี `blocked=true` |
| ไฟล์หลักฐาน | `14:47:03.945 d7818de7 notification - foreground … lightIdle=false posted=true reason=granted id=1` |

**สภาพแวดล้อมที่ตั้งไว้:** `POST_NOTIFICATIONS` / `BLUETOOTH_SCAN` /
`ACCESS_FINE_LOCATION` = granted · battery `deviceidle whitelist` = อยู่ในรายการ
(`user,com.beaconkit.example,10245`) · `standby-bucket = 5 (EXEMPTED)` ·
build ที่ติดตั้ง `sha256 2f1c5cfa91a9d36c7ec2…`

**ยังไม่ได้ทำบนเครื่องนี้:** MIUI Autostart (ไม่มีคำสั่ง adb ต้องเปิดในแอป Security เอง)
· รอบเดินทดสอบ proximity ตามขั้น 4 ของ brief · ผล proximity ทั้งหมดในไฟล์นี้ยังเป็นของ
Redmi Note 9 เท่านั้น

---

### ⛔ notification ถูกระบบบล็อก — ข้อ 3ก ยังพิสูจน์ไม่ได้ (พบ 9 ก.ย. 2026 14:16) · **เครื่องที่ 1 (Redmi Note 9) เท่านั้น**

**ข้อเท็จจริงที่ตรวจได้จากเครื่อง:**

1. `adb shell dumpsys notification` → `AggregatedStats{ key='com.beaconkit.example',
   numEnqueuedByApp=10, numPostedByApp=0, numBlocked=10 }` — แอป**ยื่น**ครบแต่**ไม่ถูกโพสต์
   สักใบ**
2. `AppSettings: com.beaconkit.example (10144)` **ไม่มีฟิลด์ `importance=` และ `userSet=`**
   ต่างจากแอป sideload ตัวอื่นบนเครื่องเดียวกันที่ผู้ใช้เคยกดอนุญาต เช่น
   `dev.firebase.appdistribution (10263) importance=DEFAULT userSet=true`
   → ผู้ใช้**ไม่เคยอนุญาต** notification ให้แอปนี้ และค่าตั้งต้นของ MIUI คือปิด
3. Settings → Notifications → App notifications → sort **"Turned off"** → **มี `example`
   อยู่ในรายการนั้นจริง**
4. หน้า per-app notification settings **เป็นสีเทาทั้งหน้า กดไม่ได้แม้แต่สวิตช์ `Show
   notifications`** (ยืนยันด้วยการ `adb shell input tap` ที่พิกัดสวิตช์แล้วสถานะไม่เปลี่ยน)
5. **ไม่ใช่** DND (`zen_mode=0`) · **ไม่ใช่** battery saver (`low_power=0`) · **ไม่ใช่**
   app suspended (`suspended=false stopped=false`) · `appops POST_NOTIFICATION = allow`
   · channel `beacon_kit_example.region_events` มีอยู่จริงและ `mImportance=3 mDeleted=false`
6. ลองติดตั้งใหม่โดยระบุ installer (`adb install -r -i com.android.vending`) แล้ว
   `installerPackageName=com.android.vending` — **สวิตช์ยังเทาเหมือนเดิม**

**ผลต่อการอ่านผลรอบทดสอบ:** บรรทัดหลักฐานในไฟล์ log **ไม่ได้รับผลกระทบเลย** (เขียนลง
`filesDir` ตรง ๆ ไม่ผ่าน NotificationManager) ข้อ 1, 2, 3, 4, 5, 6 ที่อ้างบรรทัด log จึงยัง
ยืนยันได้ตามเดิม — สอดคล้องกับกติกาที่ `ExampleNotifications` เขียนไว้เองว่า "log คือหลักฐาน
notification ไม่ใช่" **แต่เกณฑ์ "ต้องเห็น notification" ของข้อ 3 ยังพิสูจน์ไม่ได้บนเครื่องนี้**
และคำบอกเล่าของผู้ทดสอบที่ว่าเห็น notification เด้งระหว่างรอบที่ 1 **ยังอธิบายไม่ได้ด้วย
ข้อมูลที่มี** (สมมติฐานที่ยังไม่ได้พิสูจน์: MIUI ปิดให้เองหลังแอปยิงถี่ 13 ใบใน 15 นาที)
— ห้ามนับเป็นหลักฐานจนกว่าจะปลดบล็อกแล้วเดินซ้ำ

**ยังไม่ได้ลอง:** รีบูตเครื่องแล้วดูสวิตช์อีกครั้ง · ถอนแล้วติดตั้งใหม่ทั้งหมด (จะล้างไฟล์
หลักฐานและต้องเปิดแอปลงทะเบียน region ใหม่ — ก๊อปไฟล์ log ออกมาก่อนทุกครั้ง)

⚠️ **ข้อ 7 (รอบที่ 1) — สิ่งที่พบและแก้ไปแล้วด้วย `staleAfter=60s`:** ผู้ทดสอบได้ notification **13 ใบใน 15 นาที**
ทั้งที่ยืนอยู่กับที่ (near → immediate → near วน) เพราะ `staleAfter = 10s` สั้นกว่าช่วงห่างระหว่าง
batch จริง (มัธยฐาน 12.1-17.5 วินาที · เกิน 10 วินาที 38 จาก 52 ช่องว่าง) gate จึงล้าง state ทิ้ง
ครึ่งหนึ่งของทุก event (31 จาก 63 บรรทัดเป็น `reason=stale`) `from` เป็น `none` แทบทุกครั้ง และ
**hysteresis 3.0/5.0 ไม่เคยได้ทำงานเลยสักครั้งตลอดรอบ** — แก้ด้วยการขยับ `staleAfterMillis`
เป็น 60_000L (ADR-20 หัวข้อ 7) ซึ่ง**ยังไม่ได้ทดสอบซ้ำ**

---

## การตั้งค่าบีคอน K9P ที่ใช้ทดสอบ — **ข้อเท็จจริง ณ 11 ก.ย. 2026**

บันทึกจากแอป **KBeacon** โดยผู้ทดสอบ (Ball) · **ค่าเหล่านี้เป็นเงื่อนไขของผลทุกรอบ
ตั้งแต่ 10 ก.ย. 16:35 เป็นต้นไป** — ดูกฎ "ช่องบังคับ" ในตารางสภาพแวดล้อมข้างบน

| รายการ | ค่า |
|---|---|
| จำนวนบีคอน | 2 ตัว |
| `major/minor` | **9902/2** และ **9903/3** |
| advertising interval | **300 ms** (ปรับจาก 1000 ms เมื่อ 10 ก.ย. 16:35) |
| **measured power ที่แอปแสดง** | **−59 dBm ทั้งสองตัว** — เป็น**ค่า default จากโรงงาน ผู้ทดสอบไม่ได้แก้** |
| password | **ค่าโรงงาน** — ⚠️ **accepted risk ของ POC · ต้องเปลี่ยนก่อน pilot** |

> ⚠️ **password โรงงานถูกยอมรับไว้โดยตั้งใจในระดับ POC เท่านั้น** — ใครก็ตามที่อยู่ใน
> ระยะ BLE และรู้ค่า default ของรุ่นนี้ เขียนทับ UUID/major/minor/TX power ของบีคอน
> ได้ทั้งหมด **ห้ามนำสภาพนี้ขึ้น pilot หรือสาขาจริงเด็ดขาด** · บันทึกไว้ที่นี่เพื่อไม่ให้
> มันเงียบหายไปพร้อมกับการที่ POC ผ่าน

### ✅ ค่าในเฟรม **สอดคล้อง**กับค่าในแอป — เฟิร์มแวร์ชดเชยเอง (แก้ 11 ก.ย. 2026)

> **ถอนแล้ว — ข้อสรุปเดิมของ PR #26 เรื่อง "ค่าในเฟรมไม่ตรงกับค่าในแอป" ผิด ถอนออกทั้งหมด**
>
> ถ้อยคำต้นฉบับคำต่อคำอ่านได้ที่:
> ```bash
> git show c0adb8a:docs/test-checklists/android_background_scanning.md
> ```
> **ไม่ยกมาไว้ที่นี่โดยตั้งใจ** — ข้อสรุปที่ถอนแล้วไม่ควรอ่านได้โดยไม่เห็นบริบทว่ามัน
> ถูกถอน · `git show` ให้ทั้งถ้อยคำเดิมและช่วงเวลาที่มันเคยมีผล
>
> **สาเหตุที่ผิด:** ตีความว่าตัวเลขสองตัวต้องเท่ากัน ทั้งที่มันวัดคนละอย่างโดยการออกแบบ

| ค่า | ตัวเลข | คืออะไร |
|---|---|---|
| measured power (แอป KBeacon) | **−59 dBm** | RSSI ที่ควรวัดได้ที่ 1 เมตร **โดยอ้างอิงกำลังส่ง 0 dBm** |
| radio TX power (แอป KBeacon) | **+8 dBm** | กำลังส่งจริงของวิทยุ |
| **byte `txPower` ในเฟรม iBeacon** | **−51 dBm** | ค่าที่เฟิร์มแวร์คำนวณให้: **−59 + 8 = −51** |

**เฟิร์มแวร์ K9P ชดเชยกำลังส่งให้เองก่อนเขียนลงเฟรม** — ตัวเลขทั้งสามสอดคล้องกัน
**ไม่มีความไม่ตรงกันใด ๆ ให้แก้** และ **ไม่มีผลกระทบต่อสูตรของ ADR-19** เพราะสูตรอ่าน
byte จากเฟรม (−51) ซึ่งเป็นค่าที่ตรงกับกำลังส่งจริงอยู่แล้ว

**ยืนยันจากไฟล์หลักฐานที่ commit ไว้สองไฟล์** —
[`2026-09-11_android_overnight_reconcile_redmi.log`](../test-data/2026-09-11_android_overnight_reconcile_redmi.log)
(10 ก.ย. 08:30 → 11 ก.ย. 09:34) และ
[`2026-09-11_android_beacon_provision_redmi.log`](../test-data/2026-09-11_android_beacon_provision_redmi.log)
(11 ก.ย. 09:34 → 11:58 · ช่วงที่ครอบคลุมการ provision):

```bash
cat docs/test-data/2026-09-11_android_overnight_reconcile_redmi.log \
    docs/test-data/2026-09-11_android_beacon_provision_redmi.log \
  | grep -oE 'txPower=[^ ]+' | sort | uniq -c
```

| ค่า | จำนวนบรรทัด |
|---|---|
| `txPower=-51` | **596** (180 บรรทัดเป็นของ 11 ก.ย. = หลัง provision) |
| `txPower=n/a` | ที่เหลือ |
| **ค่าอื่นใด** | **0 — ไม่มีเลยแม้แต่บรรทัดเดียว** |

ทั้งสอง beacon tag (`55:4D`, `55:50`) ให้ค่าเดียวกัน

> ⚠️ **ตัวเลขชุดนี้ถูกนับใหม่ 11 ก.ย. 2026 หลังพบว่าเอกสารรุ่นก่อนอ้าง "436/179 บรรทัด"
> จากไฟล์ที่ดึงจากเครื่องแต่ **ไม่เคยถูก commit**** — ตรวจกลับไม่ได้เลย ซึ่งผิดกติกา
> citation ของโปรเจกต์เอง · ไฟล์ช่วง 09:34-11:58 ถูก commit เพิ่มในรอบนี้เพื่อให้
> ทุกตัวเลขข้างบนรันคำสั่งซ้ำได้จริง

> ### 🧾 คำถามเดียวที่ยังเปิดอยู่ — **รอ calibration ขั้น 1**
>
> **"−51 dBm ตรงกับ RSSI ที่วัดได้จริงที่ระยะ 1 เมตรหรือไม่"**
>
> ตัวเลขในเฟรม**สอดคล้องกันเองแล้ว** แต่ยังไม่มีใครวัดว่ามันตรงกับความเป็นจริงใน
> สภาพแวดล้อมที่ใช้งานจริงหรือเปล่า — ค่า default จากโรงงานคือค่าที่ผู้ผลิตวัดใน
> ห้องทดลอง ไม่ใช่ในออฟฟิศที่มีผนัง โต๊ะ และคน
>
> **นี่คือคำถามของ calibration ขั้น 1 ไม่ใช่บั๊ก** — ดูโปรโตคอลข้างล่าง

### 📐 โปรโตคอล calibration — ขั้นที่ 1 (ยังไม่ได้ทำ)

**เป้าหมาย:** ตอบว่าค่า `txPower` ที่อยู่ในเฟรมตรงกับ RSSI จริงที่ 1 เมตรไหม
**ก่อน**จะไป calibrate `n` — เพราะถ้าขั้นนี้เพี้ยน การ calibrate `n` จะไปดูดซับ
ความผิดพลาดเข้ามาไว้ใน `n` แล้วได้ค่าที่พอดีกับระยะที่ใช้ calibrate แต่ผิดที่ระยะอื่น

1. วางบีคอนและเครื่องห่างกัน **1.00 เมตร** เส้นตรง ไม่มีสิ่งกีดขวาง ความสูงเท่ากัน
2. เก็บ `rssi=` จากไฟล์หลักฐานอย่างน้อย **30 sample ต่อบีคอน** แล้วหา**มัธยฐาน**
   (ไม่ใช่ค่าเฉลี่ย — RSSI มี outlier เป็นปกติ)
3. **เทียบมัธยฐานกับ `txPower=` ที่อยู่ใน _บรรทัดเดียวกันของไฟล์หลักฐาน_
   — ไม่ใช่กับ measured power ที่แอป KBeacon แสดง**
   > 🔴 **ข้อนี้คือหัวใจของขั้นที่ 1** สองค่านั้นต่างกัน 8 dB โดยการออกแบบ
   > (แอปอ้างอิง 0 dBm ส่วนเฟรมรวมกำลังส่งจริงไว้แล้ว) · ถ้าเทียบกับค่าในแอป
   > จะสรุปว่า "เพี้ยน 8 dB" ทั้งที่ไม่มีอะไรเพี้ยนเลย — **ความผิดพลาดนี้เกิดขึ้น
   > จริงแล้วหนึ่งครั้งใน PR #26** และเป็นเหตุผลที่ข้อนี้ถูกเขียนเป็นขั้นตอนบังคับ
4. บันทึกส่วนต่าง (มัธยฐาน − `txPower` ในเฟรม) ต่อบีคอนต่อสถานที่ลง
   `docs/beacon-inventory.md`
5. **ถ้าส่วนต่างเกิน ±3 dB** ให้ถือว่าค่าในเฟรมใช้เป็น reference ไม่ได้ในสถานที่นั้น
   → ต้องตัดสินว่าจะแก้ค่าในบีคอนหรือชดเชยในโค้ด **(เป็นการตัดสินใจระดับ ADR)**

⚠️ **ขั้นที่ 1 ยังทำไม่ได้จนกว่ารอบข้ามคืนคืนที่ 11 ก.ย. จะจบ** (ดูกล่องถัดไป)

> ### 🔒 **ห้ามแก้ค่าใดในบีคอนก่อนรอบข้ามคืนคืนที่ 11 ก.ย. 2026 จบ**
>
> รอบข้ามคืนคืนนี้เป็น **รอบที่ 3** ของเคส `reconcile()` ข้ามคืน (แถว 10) และคุณค่า
> ทั้งหมดของมันคือ**การเทียบกับรอบที่ 1-2 ได้** ซึ่งเป็นไปได้ก็ต่อเมื่อ
> **ตัวแปรฝั่งบีคอนไม่เปลี่ยนเลย** — interval 300 ms · measured power −59 (เฟรม −51) ·
> major/minor เดิม
>
> บทเรียนนี้มีราคามาแล้ว: การปรับ interval 1000→300 ms เมื่อ 10 ก.ย. 16:35 ทำให้
> ตัวเลขสองรอบของ ADR-21/ADR-22 **เทียบกันไม่ได้จนถึงทุกวันนี้** และยังต้องเดิน A/B
> เพิ่มเพื่อปิดสมมติฐาน (ดู `ios_broadcast_scanning.md` หัวข้อ "interval ของบีคอน")
>
> **การแก้ค่าใด ๆ ในบีคอนคืนนี้ = เสียรอบที่ 3 ทิ้ง** งาน calibration ทั้งหมดเริ่มได้
> **หลัง**ดึง log ของเช้าพรุ่งนี้แล้วเท่านั้น

## ข้อสังเกต — **นาฬิกาปลุก `exit` ถูกระงับข้ามคืนจริง** `observed` (2 รอบ)

**ไม่ใช่ทฤษฎี ไม่ใช่การอนุมานจากความเงียบ** — วัดได้จากการที่ `exitReason=alarm`
เกิดถี่มากในวันเดียวกัน แล้ว**หายไปเป็นศูนย์**เฉพาะในหน้าต่างข้ามคืน แล้วกลับมาเช้าวันรุ่งขึ้น

| | รอบ 1 (8→9 ก.ย.) | รอบ 2 (10→11 ก.ย.) |
|---|---|---|
| บีคอนถูกเห็นครั้งสุดท้าย | 8 ก.ย. **18:09:47** | 10 ก.ย. **~18:10:27** (จาก `sinceLastSeenMs`) |
| `exitReason=alarm` ครั้งสุดท้ายก่อนเงียบ | 8 ก.ย. **18:09:28** (`standbyBucket=active`) | 10 ก.ย. ช่วงบ่าย |
| **`exitReason=alarm` ในหน้าต่างข้ามคืน** | **0 ครั้ง** | **0 ครั้ง** |
| `exitReason=alarm` ทั้งไฟล์ (ตอนกลางวัน) | **31 ครั้ง** | **62 ครั้ง** |
| กู้ได้ด้วย | `staleReconcile` 9 ก.ย. **08:36:17** | `staleReconcile` 11 ก.ย. **08:39:19** |
| `standbyBucket` ตอนกู้ | `rare` | `rare` |
| ความยาวที่ค้าง `inside` | **14 ชม. 26 นาที** | **14 ชม. 29 นาที** |

**ทั้งสองรอบเกิดบนบิลด์ที่มี ADR-17 แล้ว** — แปลว่า `reconcile()` ไม่ได้แก้ที่ต้นเหตุ
(นาฬิกาปลุกยังถูกระงับเหมือนเดิม) แต่**ทำหน้าที่ตามที่ออกแบบไว้เป๊ะ**: เป็นตาข่ายรับ
เมื่อนาฬิกาปลุกไม่มาถึง · ความสม่ำเสมอของตัวเลข (14 ชม. 26 นาที vs 14 ชม. 29 นาที ·
`rare` ทั้งคู่ · กู้ได้ 08:36 vs 08:39) **แข็งแรงกว่าการเดินซ้ำแบบตั้งใจเสียอีก**
เพราะทั้งสองรอบเกิดเองโดยไม่มีใครจัดฉาก

⚠️ **ยังไม่ใช่ `verified`** — 2 รอบบน**เครื่องเดียว รุ่นเดียว** (Redmi Note 9 / MIUI)
ยังไม่ตอบว่าเป็นพฤติกรรมของ MIUI, ของ Android 12, หรือของ App Standby `rare` ทั่วไป

### 🔗 จุดยืนยันข้ามแพลตฟอร์มของรอบ 2 — **18:10:27 / 18:10:33**

`sinceLastSeenMs=52172804` บนบรรทัด `staleReconcile` ของ **Redmi** (11 ก.ย. 08:39:19)
ชี้กลับไปที่ **10 ก.ย. 18:10:27** ซึ่งห่างจาก `reason=regionExit` ของ **iPhone**
ที่ **10 ก.ย. 18:10:33** เพียง **6 วินาที**
(`docs/test-data/2026-09-11_ios_overnight_background_proximity.log` บรรทัดแรก)

**คนละเครื่อง คนละ OS คนละกลไกตรวจจับโดยสิ้นเชิง เห็นบีคอนตัวเดียวกันหายไปในเวลาเดียวกัน**
— เป็นหลักฐานที่แข็งที่สุดว่าเหตุการณ์ที่บันทึกคือ**เหตุการณ์จริงของบีคอน** ไม่ใช่
artifact ของแพลตฟอร์มใดแพลตฟอร์มหนึ่ง · **ห้ามใช้จุดนี้สรุปเรื่อง latency หรือความแม่นของ
เวลา** — มันยืนยันแค่ว่า "เหตุการณ์เดียวกัน" ไม่ได้ยืนยันว่าใครเร็วกว่าใคร

## 📋 `bigc-test` เงียบ 10 ก.ย. แล้วกลับมา 11 ก.ย. — **เพราะคนย้ายฮาร์ดแวร์ ไม่ใช่บีคอน "กลับมาเอง"**

> **ถอนแล้ว — ข้อสรุปเดิมของ PR #26 เรื่อง `bigc-test` ผิดทั้งสามข้อ ถอนออกทั้งหมด**
>
> ถ้อยคำต้นฉบับคำต่อคำอ่านได้ที่:
> ```bash
> git show c0adb8a:docs/test-checklists/android_background_scanning.md
> ```
> **ไม่ยกมาไว้ที่นี่โดยตั้งใจ** ด้วยเหตุผลเดียวกับกล่องถอนข้อสรุปเรื่อง `txPower`
>
> **สิ่งที่เกิดจริง:** คนจัดการฮาร์ดแวร์ โดยที่ไฟล์หลักฐานไม่มีทางรู้ — ดูตารางข้างล่าง

### สิ่งที่เกิดขึ้นจริง (ผู้ทดสอบยืนยัน)

| บีคอน | เหตุการณ์ |
|---|---|
| **#1 (`9901/1`)** | **อยู่กับหัวหน้าตั้งแต่เช้า 10 ก.ย.** — ไม่ได้อยู่ในสนามทดสอบเลยตั้งแต่นั้น |
| **#2 (`9902/2`)** · **#3 (`9903/3`)** | **ถูก provision เป็น UUID ของ BigC เมื่อ 11 ก.ย. ~11:30** · `major`/`minor` **คงเดิม** |

**ผลที่ตามมาโดยตรงต่อการอ่านไฟล์หลักฐาน:**

**ตรวจสอบได้จาก** [`2026-09-11_android_beacon_provision_redmi.log`](../test-data/2026-09-11_android_beacon_provision_redmi.log)
(180 บรรทัด · md5 `212804928b8d96fa39095404452c40c5`):

- `bigc-test` ปรากฏเป็นค่าคอลัมน์ region **25 บรรทัด บรรทัดแรกที่ 11:32:44**
  — ก่อนหน้านั้น **0 บรรทัด**ตลอดไฟล์ข้ามคืน (ที่นั่น `bigc-test` โผล่เฉพาะใน
  `restoredRegions=[k9p-default,bigc-test]` ของบรรทัด `launch` ซึ่ง**ไม่ใช่ event**)
  · ยืนยันว่า **#2 และ #3 ย้ายมาอยู่ใต้ UUID นี้จริง**
- **`k9p-default` หยุดมี `proximity` ตั้งแต่ 11:10:44** และเหลือเพียงบรรทัดเดียวหลัง
  11:32 คือ **`exit` ที่ 11:33:45** แล้วเงียบสนิท — **ซึ่งเป็นสิ่งที่ควรเกิดพอดี**
  เมื่อบีคอนทุกตัวออกจาก UUID เดิม **ไม่ใช่สัญญาณผิดปกติ**
- **12 จาก 25 บรรทัดของ `bigc-test` เป็น `bucket=immediate`** — คือช่วงที่ผู้ทดสอบ
  **ถือบีคอนอยู่ในมือตอน provision** · **ไม่ใช่การเดินทดสอบ ห้ามนับเข้าสถิติระยะ
  หรือ latency ใด ๆ**

### ความเงียบ 10 ก.ย. → 11 ก.ย. เช้า

อธิบายได้ด้วย **#1 ไม่อยู่ในสนาม** บวกกับ **ไม่มีใครพา #2/#3 เข้าใกล้เครื่องทดสอบ**
— ไม่ใช่ "บีคอนอยู่นอกระยะโดยบังเอิญ" แบบที่เอกสารเดิมสรุป และไม่ใช่บั๊กของ SDK

<details>
<summary>🎓 บทเรียนที่ต้องไม่ทำซ้ำ — อ่านก่อนสรุปอะไรจากไฟล์หลักฐาน</summary>

**"0 บรรทัด" กับ "25 บรรทัดที่กลับมา" อ่านผิดได้เท่า ๆ กัน ถ้าไม่มีบันทึกว่าคนทดสอบ
ทำอะไรกับฮาร์ดแวร์ตอนไหน**

ในเคสเดียวกันนี้ ผมสรุปผิดสองครั้งติดกันในทิศตรงข้ามกัน:

1. **เห็น 0 บรรทัด** → สรุปว่า "บีคอนอาจตาย/แบตหมด" → เปิดเป็นเคสให้ไปตรวจฮาร์ดแวร์
   ทั้งที่ความจริงคือ **บีคอนตัวนั้นอยู่กับคนอื่นคนละที่มาตั้งแต่วันก่อน**
2. **เห็น 25 บรรทัดกลับมา** → สรุปว่า "กลับมาเอง บีคอนปกติ ไม่ต้องตรวจ"
   ทั้งที่ความจริงคือ **มันเป็นบีคอนคนละตัวที่เพิ่งถูก provision ให้ UUID ใหม่**
   และบรรทัดที่เห็นคือมือคนถือบีคอนอยู่ ไม่ใช่การทดสอบ

**สิ่งที่ไฟล์หลักฐานบอกได้:** อะไรถูกตรวจพบ เมื่อไร แรงแค่ไหน
**สิ่งที่ไฟล์หลักฐานบอกไม่ได้เลย:** บีคอนตัวไหนอยู่ที่ไหน ใครถืออยู่ ถูกตั้งค่าใหม่
เมื่อไร มีใครเดินไปทางนั้นหรือเปล่า — **ทั้งหมดนี้คือตัวแปรต้น ไม่ใช่รายละเอียดปลีกย่อย**

**กติกาที่ออกมา:**
- ทุกครั้งที่ตัวเลขในไฟล์หลักฐาน "เปลี่ยนพฤติกรรม" **ต้องถามว่ามีอะไรเปลี่ยนทาง
  ฮาร์ดแวร์/คนไหม ก่อนจะตั้งสมมติฐานทางซอฟต์แวร์**
- ความเคลื่อนไหวของบีคอนทุกตัวต้องถูกบันทึกที่ [`docs/beacon-inventory.md`](../beacon-inventory.md)
  พร้อมวันที่และผู้ทำ — ไฟล์นั้นถูกสร้างขึ้นเพราะเคสนี้โดยตรง

</details>

## หมายเหตุถาวร — วิธีอ่าน `exitReason=staleReconcile` (ADR-17)

> ⚠️ **`timestampMillis` ของบรรทัดที่มี `exitReason=staleReconcile` คือเวลาที่
> `reconcile()` "ตรวจพบ" ว่าสถานะค้าง ไม่ใช่เวลาที่อุปกรณ์ออกนอกโซนจริง** — สอง
> เวลานี้ต่างกันได้มาก (ในเคสจริงที่เคยเจอคือหลายชั่วโมง เพราะนาฬิกาปลุกถูกระงับไว้
> ทั้งคืน) **ค่าที่บอกความจริงเรื่อง "หายไปนานแค่ไหน" คือ `sinceLastSeenMs` เท่านั้น**
> ห้ามใช้ `timestampMillis` ลบกับเวลาที่คาดว่าออกนอกโซนมาคำนวณ dwell/gap — ต้องอ่าน
> `sinceLastSeenMs` ตรง ๆ จากบรรทัดนั้นเสมอ (ดูรายละเอียดฟิลด์เต็มใน
> `android_background_runbook.md` §0.2 ตาราง "ฟิลด์ในคอลัมน์สัญญาณดิบ" แถว
> `exitReason`) — บันทึกไว้ในไฟล์นี้เพราะเป็นจุดที่คนอ่านผลทีหลังตีความผิดง่ายที่สุด
> ถ้าไม่มีบรรทัดนี้กำกับ

---

## ตารางผล

| ข้อ | เคส | runbook | สถานะ | เวลา enter | เวลา exit | รอบที่ไม่มี event | หมายเหตุ |
|---|---|---|---|---|---|---|---|
| 1 | แอปอยู่เบื้องหลัง (กด home) | §2 | **observed** — [`2026-09-03_android_overnight_stale_inside.log`](../test-data/2026-09-03_android_overnight_stale_inside.log) บรรทัด 253-260 (`procUuid=f509dd45`): 4 `exit` + 4 `enter` สลับกันตลอด 08:42:29-08:47:23 น. (4 ก.ย.) `procUuid` เดิมทุกบรรทัด ไม่มี `launch` ใหม่คั่นกลาง และ `conclusion=background` (ไม่ใช่ `relaunchedFromTerminated`) ทุกบรรทัด — ตรงตามเกณฑ์ที่ runbook §2 ข้อ 7 กำหนด (process เดิม ไม่ถูกฆ่า) | — | — | 1 รอบ (ยังไม่ครบเกณฑ์ 2-3 รอบของ runbook) | **เกิดขึ้นเองระหว่างเปิดแอปตอนเช้าตามลำดับ §11 (ADR-17) ไม่ใช่การเดินตาม protocol §2 ที่ตั้งใจ** — ไม่มีการกด home โดยตั้งใจ ไม่มีนาฬิกาแยกจับเวลา ไม่มีการถอด/ใส่แบต K9P คู่กับเวลาที่บันทึก จึงกรอกช่อง "เวลา enter/exit" ไม่ได้ · `everForeground=true` ยืนยันว่า process นี้เคย active มาก่อน (สอดคล้องกับ "state=stopped" = แค่ถูกส่งไปพื้นหลัง ไม่ตาย) · ยังต้องทำซ้ำตาม protocol §2 จริงอีก 2-3 รอบ (ดูหมายเหตุท้ายตาราง) |
| 2 | ปัดแอปทิ้งจากรายการแอป | §3 | ยังไม่ทดสอบ | | | | เคสสำคัญที่สุด — เทียบตรงกับ B5 ของ iOS · log สองไฟล์ที่มีไม่ได้เดินตาม protocol นี้ (ไม่มีการปัดแอปทิ้งโดยตั้งใจ) — ห้ามเดาว่าเป็นเคสนี้จาก `relaunchedFromTerminated` เฉย ๆ เพราะ MIUI ฆ่า process เองได้โดยไม่มีใครปัด |
| 3 | force-stop | §4 | ยังไม่ทดสอบ | | | | **คาดว่าไม่รอด** — ยืนยันว่าไม่รอดจริงคือผลที่ถูกต้อง · ต้องมีคนกด "บังคับหยุด" เองที่เครื่อง ไม่มีในหลักฐานที่มี |
| 4 | รีบูตแล้วไม่เปิดแอป | §5 | ยังไม่ทดสอบ | | | | ต้องแยกกรณีปลดล็อกแล้ว / ยังไม่ปลดล็อก — ต้องมีคน `adb reboot` เองแล้วจับเวลา ไม่มีในหลักฐานที่มี |
| 5 | Doze | §6 | **observed (บางส่วน)** — [`2026-09-03_android_overnight_stale_inside.log`](../test-data/2026-09-03_android_overnight_stale_inside.log) มีบรรทัด `exit` ที่ `doze=true` รวม 11 บรรทัด (`receiverEntry=true`, `conclusion=relaunchedFromTerminated`, 10 `procUuid` ต่างกัน) กระจายตั้งแต่ 20:04 น. 2 ก.ย. ถึง 09:09 น. 3 ก.ย. — ยืนยันว่านาฬิกาปลุก `PendingIntent` ยังดังและ `BroadcastReceiver` ยังรับ event ได้จริงระหว่างเครื่องอยู่ใน Doze (`PowerManager.isDeviceIdleMode()=true` ณ ตอนเขียนบรรทัด) | — | ล่าช้า 37,905-239,523 ms (`sinceLastSeenMs`) เทียบกับ `exitTimeoutSeconds=30` (30,000 ms) ที่ขอ | 0 (ทุกบรรทัดที่กรองมามี event ครบ) | **ตอบได้แค่ "นาฬิกาปลุกรอด Doze หรือไม่" เท่านั้น — ไม่ได้เดินตาม §6.1/§6.2 เต็มรูปแบบ**: ไม่มีการถอด/ใส่แบต K9P พร้อมนาฬิกาแยกจับเวลาความหน่วงจากจุดที่ K9P ออกนอกระยะจริง (ตัวเลข `sinceLastSeenMs` วัดจาก `lastSeen` ภายในแอปเอง ไม่ใช่จากนาฬิกาแยก) และไม่มีคีย์ `standbyBucket` ในบรรทัด log รุ่นนี้ จึงยืนยัน App Standby bucket ระหว่างนั้นไม่ได้ (ต่างจากที่ §0.1 ข้อ 6 ต้องการ) · **ไฟล์ 09-01 ใช้คำนวณ delay ไม่ได้** (ดูหมายเหตุความต่างของสองไฟล์ด้านบน) · ยังต้องทำซ้ำตาม §6.2 จริง (ดูหมายเหตุท้ายตาราง) |
| 6 | MIUI Autostart เปิด vs ปิด | §7 | ยังไม่ทดสอบ | | | | ตอบข้อสงสัยที่ ADR-12 หัวข้อ 6 ทิ้งไว้ — ต้องมีคนสลับ Autostart เองทีละตัวแปร ไม่มีในหลักฐานที่มี (log สองไฟล์ไม่ได้บันทึกว่า Autostart เปิดหรือปิดระหว่างคืนนั้น) |
| 7 | ยืนยัน `FLAG_MUTABLE` | §8 | ยังไม่ทดสอบ | — | — | | ADR-14 หัวข้อ 2.1 เป็นการอนุมานจากเอกสาร ยังไม่ใช่ผลการทดลอง — ต้องแก้โค้ดชั่วคราวเป็น `FLAG_IMMUTABLE` แล้วทดสอบเทียบ ไม่มีในหลักฐานที่มี |
| 8 | สวิตช์ Location ของระบบ | §9 | ยังไม่ทดสอบ | | | | ตอบข้อสงสัยที่ ADR-12 หัวข้อ 1 ทิ้งไว้ — ต้องมีคนปิดสวิตช์ Location เองแล้วเทียบกับตอนเปิด ไม่มีในหลักฐานที่มี |
| 9 | เพดานจำนวน region พร้อมกัน | §10 | ยังไม่ทดสอบ | — | — | | ADR-14 หัวข้อ 4.1 — ต้องแก้ example app ชั่วคราวให้ส่ง region 20+ อัน ไม่มีในหลักฐานที่มี (log สองไฟล์มีแค่ 2 region: `k9p-default`, `bigc-test`) |
| 10 (ใหม่) | ออกนอกระยะข้ามคืน — `reconcile()` (ADR-17) | §11 | **`observed` (2 รอบ)** | Redmi Note 9 · `standbyBucket=rare` ทั้งสองรอบ · `battOpt=optimized` | [`2026-09-09_android_proximity_background.log`](../test-data/2026-09-09_android_proximity_background.log) · [`2026-09-11_android_overnight_reconcile_redmi.log`](../test-data/2026-09-11_android_overnight_reconcile_redmi.log) | ดูคอลัมน์ถัดไป | **`reconcile()` กู้สถานะที่ค้าง `inside` ข้ามคืนได้จริง — ทำซ้ำได้ 2 รอบ ห่างกัน 2 วัน และรูปร่างเหมือนกันอย่างน่าทึ่ง:**<br>**รอบ 1 — 9 ก.ย. 08:36:17** `exit bigc-test` · `exitReason=staleReconcile` · `sinceLastSeenMs=51989899` = **14 ชม. 26 นาที** · `rare` · `relaunchedFromTerminated` (บีคอนถูกเห็นครั้งสุดท้าย 8 ก.ย. 18:09:47)<br>**รอบ 2 — 11 ก.ย. 08:39:19** `exit k9p-default` · `exitReason=staleReconcile` · `sinceLastSeenMs=52172804` = **14 ชม. 29 นาที** · `rare` · `relaunchedFromTerminated` · launch→exit ห่างกันเพียง **111 ms** แล้วตามด้วย `enter` ทันที<br>⚠️ อ่าน `sinceLastSeenMs` เท่านั้น **ห้ามใช้ `timestampMillis` คำนวณ gap** (ดูหมายเหตุถาวรข้างบน) · **2 รอบยังไม่ครบเกณฑ์ `verified`** ตาม `android_background_runbook.md` |

> ### 🔍 ทำไมรอบที่ 1 (8-9 ก.ย.) ถึงตกหล่นไปสองครั้ง — **สาเหตุเชิงระบบ ไม่ใช่ความเลินเล่อครั้งเดียว**
>
> หลักฐานของรอบที่ 1 **อยู่ในไฟล์ที่ commit ไว้ตั้งแต่ 9 ก.ย. แล้ว** แต่ถูกมองข้ามสองรอบ
> ด้วยคนละเหตุผล — ทั้งสองเหตุผลเป็นกลไกที่จะเกิดซ้ำได้ถ้าไม่แก้:
>
> **ครั้งที่ 1 (ตอนเขียนแถวนี้):** เหตุผลที่บันทึกไว้ว่า "ยังไม่ทดสอบ" คือ
> *"ทั้งสองไฟล์ [ข้ามคืน] เป็นบิลด์ก่อน ADR-17 · `grep -c "exitReason=" <ไฟล์>` = 0"*
> ซึ่ง**ถูกต้องสำหรับสองไฟล์ที่ตรวจ** (`2026-09-03_android_overnight_stale_inside.log`
> และไฟล์ iOS คู่ของมัน) — แต่ไฟล์ `2026-09-09_android_proximity_background.log`
> **ไม่เคยถูกนำมาตรวจเลย** เพราะมันถูกจัดประเภทในดัชนี (`docs/test-data/README.md`)
> ว่าเป็น "หลักฐานของ ADR-20 (proximity)" · **ไฟล์ถูกค้นตามเหตุผลที่เก็บ ไม่ใช่ตามสิ่งที่
> มันพิสูจน์ได้** ทั้งที่มันครอบคลุม 8 ก.ย. 14:48 → 9 ก.ย. 13:50 = ข้ามคืนเต็ม ๆ
>
> **ครั้งที่ 2 (ตอนตรวจ log 11 ก.ย.):** ยืนยันว่าไฟล์นั้นมี `staleReconcile` 3 บรรทัด
> จาก `grep -c` แล้ว **แต่สรุปลักษณะของมันจากไฟล์คนละไฟล์** — เอาค่า `sinceLastSeenMs`
> และ `standbyBucket` ของ 3 บรรทัดในไฟล์ Redmi 10-11 ก.ย. (ซึ่งบังเอิญมี 3 บรรทัดเท่ากัน
> และสองในสามเป็นเคสกลางวัน `workingSet`) มาอ้างเป็นของไฟล์ 9 ก.ย. **โดยไม่เคยเปิดดู
> บรรทัดจริงเลย** แล้วสรุปว่า "ทั้งสามเป็นเคสกลางวัน" ซึ่งผิด — ของจริงคือ
> `rare` ทั้งสามบรรทัด และบรรทัดแรกมี `sinceLastSeenMs` **14 ชม. 26 นาที**
>
> **กติกาที่ออกมาจากเรื่องนี้:** `grep -c` บอกได้แค่ว่า *"มีกี่บรรทัด"*
> **ห้ามสรุปลักษณะของบรรทัดโดยไม่พิมพ์บรรทัดนั้นออกมาดู** — และเมื่อจะตอบว่าเคสใด
> "ยังไม่ทดสอบ" ต้อง**ค้นไฟล์หลักฐานทุกไฟล์ที่ช่วงเวลาครอบคลุม ไม่ใช่เฉพาะไฟล์ที่ถูก
> ตั้งชื่อ/จัดประเภทว่าเกี่ยวข้อง**

**ยังต้องทำก่อนเปลี่ยนคำของแถว 1/5 เป็นคำที่แข็งแรงกว่า `observed`:**

- **แถว 1 (§2):** ทำซ้ำตาม protocol §2 จริงอีกอย่างน้อย 2-3 รอบ (กด home โดยตั้งใจ + นาฬิกาแยกจับเวลาถอด/ใส่แบต K9P) — ตอนนี้เห็นแค่ 1 รอบและเป็นเหตุการณ์ที่เกิดขึ้นเองระหว่างเก็บ log เคสอื่น ไม่ใช่รอบที่ตั้งใจทดสอบ §2 โดยตรง
- **แถว 5 (§6):** ทำ §6.1 (บังคับ Doze ด้วย `adb`) และ §6.2 (แบบจริง — ถอด/ใส่แบต K9P พร้อมนาฬิกาแยก) ตามขั้นตอนเต็มรูปแบบ แล้ววัด `sinceLastSeenMs` เทียบกับเวลาที่นาฬิกาแยกจับได้จริง — ตัวเลขที่มีตอนนี้มาจากความเงียบตามธรรมชาติของการวางเครื่องนิ่งข้ามคืน ไม่ใช่การวัดที่ควบคุมจุดเริ่มต้น (K9P ออกนอกระยะเมื่อไร) ได้แม่นยำ

---

## สิ่งที่ยืนยันแล้ว — และขอบเขตของมัน

รายการนี้ **ไม่ใช่หลักฐานว่าฟีเจอร์ทำงาน** ตาม CONTRIBUTING ข้อ 4:
"unit test ที่เขียวไม่นับเป็นการยืนยัน สำหรับอะไรก็ตามที่คุยกับ OS หรือฮาร์ดแวร์"

| สิ่งที่ยืนยันแล้ว | ยืนยันด้วยอะไร | **พิสูจน์อะไรไม่ได้** |
|---|---|---|
| ไบต์ของ `ScanFilter` เรียงตรงตามสเปก iBeacon (UUID / major / minor / mask) | `BeaconRegionSpecTest` (6 เทสต์ · JVM) | ว่าตัวกรองนี้จะ match beacon จริงบนอากาศ |
| รูปแบบบรรทัด log 6 คอลัมน์ตรงกันทั้งสองแพลตฟอร์ม | `BackgroundEvidenceLogTest` (Kotlin) + `testLogLineHasSixTabSeparatedColumnsInOrder` (Swift) + `evidence_log_line_test.dart` | ว่าไฟล์จะถูกเขียนได้จริงตอน process ถูกปลุก |
| ตัววิเคราะห์ยังอ่าน log รูปแบบเก่า 5 คอลัมน์ได้ | รันจริงกับ `docs/test-data/2026-08-30_overnight_region_flapping.log` (176 บรรทัด) | — |
| สัญญาที่ข้าม method/event channel | `background_region_monitoring_test.dart` (8 เทสต์) | ว่า Android จะปลุก process ขึ้นมาส่งผลสแกนจริง |
| ตัวรับ broadcast ทั้ง 3 ตัว + `RECEIVE_BOOT_COMPLETED` merge เข้า manifest ของแอปจริง | ตรวจ merged manifest หลัง build | ว่าตัวรับจะถูกเรียกจริง |
| `ExampleApplication` ถูกใช้เป็น `android:name` จริง (ไม่ใช่ `${applicationName}` ค่า default) | ตรวจ merged manifest หลัง build | — |

---

## บันทึกรอบทดสอบ

**หมายเหตุ:** ตอนนี้ติดตั้งลงเครื่องได้แล้ว (ตัวบล็อก `INSTALL_FAILED_USER_RESTRICTED`
เดิมหมดอายุไปแล้ว) และมีข้อมูลจริงจากเครื่องอยู่ในตารางผลด้านบนแล้ว (แถว 1 และ 5) —
แต่นั่นคือข้อมูลที่**เกิดขึ้นเองระหว่างเก็บ log ข้ามคืนของเคสอื่น** ไม่ใช่รอบทดสอบที่
ตั้งใจเดินตาม protocol ของแต่ละเคสตามรูปแบบด้านล่างนี้ — ยังไม่มีการบันทึกรอบทดสอบ
แบบตั้งใจตามรูปแบบนี้เลยสักรอบ

รูปแบบที่ต้องบันทึกต่อรอบ:

```
วันที่/เวลา · เคสข้อที่ · Autostart · battOpt · location_mode
processId ก่อน / processId หลัง
เวลาถอดแบต → เวลาได้ exit  = ...
เวลาใส่แบต  → เวลาได้ enter = ...
บรรทัด log ที่เป็นหลักฐาน (คัดลอกมาทั้งบรรทัด)
```
